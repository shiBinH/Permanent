Permanent is a web application designed with the purpose of assisting the user to "permanently" remember concepts.


issues:

- escape file contents on server
- add a create topic function/topic class (?)
